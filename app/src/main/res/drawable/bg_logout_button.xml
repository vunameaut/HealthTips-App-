<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradient từ cam tươi sang đỏ cam cho nút logout -->
    <gradient
        android:startColor="@color/secondary_button_start"
        android:endColor="@color/secondary_button_end"
        android:angle="45" />

    <!-- Bo góc -->
    <corners android:radius="8dp" />

    <!-- Đường viền nhẹ -->
    <stroke
        android:width="1dp"
        android:color="@color/divider" />

</shape>

