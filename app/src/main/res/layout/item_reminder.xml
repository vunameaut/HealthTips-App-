<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_reminder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorOutlineVariant"
    app:cardBackgroundColor="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="top"
        android:minHeight="100dp">

        <!-- Left Icon & Status - Cố định kích thước để không đè lên -->
        <RelativeLayout
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginEnd="16dp"
            android:layout_marginTop="4dp">

            <!-- Icon Container với kích thước cố định -->
            <LinearLayout
                android:id="@+id/icon_container"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_centerInParent="true"
                android:background="@drawable/circle_background_primary"
                android:gravity="center"
                android:elevation="2dp">

                <ImageView
                    android:id="@+id/iv_reminder_icon"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_alarm"
                    android:tint="@color/white"
                    app:tint="@color/white" />

            </LinearLayout>

            <!-- Status Indicator - Vị trí cố định không đè lên icon -->
            <View
                android:id="@+id/view_status_indicator"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_alignParentTop="true"
                android:layout_alignParentEnd="true"
                android:background="@drawable/circle_background"
                android:backgroundTint="@color/success_color"
                android:visibility="visible" />

        </RelativeLayout>

        <!-- Content Area - Chiếm toàn bộ không gian còn lại -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginEnd="16dp"
            android:minHeight="56dp"
            android:gravity="center_vertical">

            <!-- Title Row - Hàng riêng cho tiêu đề -->
            <TextView
                android:id="@+id/tv_reminder_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Nhắc nhở uống thuốc"
                android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
                android:textColor="?attr/colorOnSurface"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"
                android:lineSpacingExtra="2dp"
                android:layout_marginBottom="6dp"
                tools:text="Nhắc nhở uống thuốc huyết áp và vitamin D" />

            <!-- DateTime Row - Hàng riêng cho ngày giờ -->
            <TextView
                android:id="@+id/tv_reminder_datetime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="08:00"
                android:textAppearance="@style/TextAppearance.Material3.LabelMedium"
                android:textColor="?attr/colorPrimary"
                android:background="@drawable/rounded_background_primary_light"
                android:paddingHorizontal="8dp"
                android:paddingVertical="4dp"
                android:minWidth="56dp"
                android:gravity="center"
                android:textStyle="bold"
                android:layout_marginBottom="6dp"
                tools:text="08:30" />

            <!-- Description - Hiển thị đầy đủ với nhiều dòng hơn -->
            <TextView
                android:id="@+id/tv_reminder_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:text="Uống thuốc huyết áp theo chỉ định bác sĩ"
                android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:maxLines="3"
                android:ellipsize="end"
                android:lineSpacingExtra="1dp"
                android:visibility="visible"
                tools:text="Uống thuốc huyết áp theo chỉ định bác sĩ. Nhớ uống cùng với bữa sáng để tránh tác dụng phụ" />

            <!-- Repeat Type với background đẹp hơn -->
            <TextView
                android:id="@+id/tv_repeat_type"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hàng ngày"
                android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
                android:textColor="?attr/colorSecondary"
                android:background="@drawable/rounded_background_secondary_light"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                android:textStyle="bold"
                tools:text="Hàng ngày" />

        </LinearLayout>

        <!-- Right Actions - Cố định chiều rộng để không bị lệch -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:minWidth="64dp">

            <!-- Toggle Switch -->
            <com.google.android.material.switchmaterial.SwitchMaterial
                android:id="@+id/sw_reminder_active"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:checked="true"
                android:theme="@style/Theme.SwitchMaterial" />

            <!-- Delete Button với kích thước cố định -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_delete_reminder"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:minWidth="40dp"
                android:minHeight="40dp"
                android:padding="8dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:insetLeft="0dp"
                android:insetRight="0dp"
                app:icon="@drawable/ic_delete"
                app:iconSize="16dp"
                app:iconTint="?attr/colorError"
                app:iconGravity="textStart"
                app:iconPadding="0dp" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
